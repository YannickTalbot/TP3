/*Directives au préprocesseur
=============================*/
#include "Bomberman.h"
#include "Menu.h"

void main()
{
	/*Menu menu;
	menu.addItem("Titre test!", MENU_ITEM_VOID, Position(20, 0, 60));
	menu.addItem("Une fraise!", MENU_ITEM_VOID, Position(20, 60, 60));
	menu.show();*/
	sf::RenderWindow App(sf::VideoMode(800, 600), "SFML window");

	system("pause");

}